# GUI 사용 가이드

Video Person Tracker의 그래픽 사용자 인터페이스(GUI) 사용 방법을 설명합니다.

## 📖 목차

1. [GUI 실행하기](#gui-실행하기)
2. [인터페이스 구성](#인터페이스-구성)
3. [기본 사용법](#기본-사용법)
4. [고급 기능](#고급-기능)
5. [문제 해결](#문제-해결)

## GUI 실행하기

### Windows
1. `run_gui.bat` 파일을 더블클릭
2. 또는 명령 프롬프트에서:
   ```cmd
   python run_gui.py
   ```

### Linux/Mac
1. 터미널에서:
   ```bash
   ./run_gui.sh
   ```
2. 또는:
   ```bash
   python3 run_gui.py
   ```

## 인터페이스 구성

### 1. 왼쪽 패널: 비디오 프리뷰

```
┌─────────────────────────────────────────┐
│                                         │
│         비디오 프리뷰 영역               │
│     (실시간 비디오 스트림 표시)         │
│                                         │
│  ┌───┐ Person 0 ← 인물 박스 및 ID      │
│  │   │                                  │
│  └───┘                                  │
│                                         │
└─────────────────────────────────────────┘
┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐
│웹캠 │ │파일 │ │중지 │ │리셋│
└─────┘ └─────┘ └─────┘ └─────┘
```

**기능:**
- 실시간 비디오 스트림 표시
- 감지된 얼굴 주변에 색상별 박스
- 각 인물의 ID 표시
- 컨트롤 버튼

### 2. 오른쪽 패널: 설정 및 통계

```
┌─────────────────────────────┐
│      ⚙️ 설정                │
├─────────────────────────────┤
│ 프레임 스킵: [2]  ▲▼       │
│ 인식 정확도: ├─────┤ 0.60  │
└─────────────────────────────┘

┌─────────────────────────────┐
│      📊 실시간 통계         │
├─────────────────────────────┤
│ 감지된 인물: 3명            │
│ 현재 프레임: 1250           │
│ 처리 속도: 25.3 FPS         │
│                             │
│ 인물별 출연 횟수:           │
│ ┌─────────────────────────┐ │
│ │person_0:  450회         │ │
│ │person_1:  320회         │ │
│ │person_2:  180회         │ │
│ └─────────────────────────┘ │
│                             │
│      [💾 결과 저장]         │
└─────────────────────────────┘
```

### 3. 하단: 상태 표시줄

```
┌─────────────────────────────────────────┐
│ 준비 / 웹캠 실행 중 / 파일 처리 중     │
└─────────────────────────────────────────┘
```

## 기본 사용법

### 1️⃣ 웹캠으로 실시간 추적

1. **"🎥 웹캠 시작"** 버튼 클릭
2. 카메라 권한 허용 (첫 실행 시)
3. 비디오 프리뷰에서 실시간으로 인물 감지 확인
4. 오른쪽 통계 패널에서 출연 횟수 확인
5. 완료되면 **"⏹ 중지"** 버튼 클릭
6. 필요시 **"💾 결과 저장"** 버튼으로 결과 저장

**예시 시나리오:**
```
회의 참석자 추적
1. 웹캠 시작
2. 회의 진행
3. 실시간으로 각 참석자의 화면 출현 시간 확인
4. 회의 종료 후 결과 저장
```

### 2️⃣ 비디오 파일 분석

1. **"📁 파일 열기"** 버튼 클릭
2. 비디오 파일 선택 (mp4, avi, mov, mkv 지원)
3. 자동으로 분석 시작
4. 진행 상황을 비디오 프리뷰와 통계에서 확인
5. 분석 완료 후 **"💾 결과 저장"** 클릭

**예시 시나리오:**
```
드라마 에피소드 분석
1. 파일 열기로 드라마 파일 선택
2. 프레임 스킵을 10으로 설정 (빠른 처리)
3. 분석 진행 관찰
4. 각 등장인물의 출연 횟수 확인
5. 결과를 JSON으로 저장
```

## 고급 기능

### 설정 조정

#### 1. 프레임 스킵

**의미:** 몇 프레임마다 얼굴 인식을 수행할지 결정

**값:**
- `1`: 모든 프레임 분석 (가장 정확, 가장 느림)
- `2-3`: 실시간 웹캠 권장 (균형)
- `5-10`: 파일 처리 권장 (빠름)
- `10+`: 매우 빠른 처리 (일부 출연 놓칠 수 있음)

**사용 예:**
```
실시간 웹캠: 2-3
일반 영상: 5
긴 영상 (1시간+): 10
빠른 프리뷰: 15-30
```

#### 2. 인식 정확도 (Tolerance)

**의미:** 얼굴을 같은 사람으로 인식하는 기준

**값:**
- `0.4-0.5`: 매우 엄격 (쌍둥이도 구분, 같은 사람을 다르게 인식 가능)
- `0.6`: 권장 설정 (균형)
- `0.7-0.8`: 느슨함 (비슷한 사람을 같게 인식 가능)

**조정 기준:**
```
같은 사람이 여러 명으로 나뉨 → tolerance 증가 (0.65-0.7)
다른 사람이 같은 사람으로 인식됨 → tolerance 감소 (0.5-0.55)
```

### 실시간 모니터링

#### 통계 해석

**감지된 인물: N명**
- 지금까지 감지된 고유한 인물의 수
- Person 0, Person 1, ... Person N-1로 표시

**현재 프레임: N**
- 처리 중인 프레임 번호
- FPS × 초 = 프레임 번호

**처리 속도: N FPS**
- 초당 처리하는 프레임 수
- 높을수록 실시간에 가까움
- 일반적으로 15-30 FPS면 원활

**인물별 출연 횟수**
```
person_0:  450회 (프레임 0-8950)
  ↑        ↑         ↑
 ID      출연횟수   첫-마지막 프레임
```

### 결과 저장 및 활용

**저장 형식:**
```json
{
  "person_0": {
    "appearances": 450,
    "frames": [0, 2, 4, 6, ...],
    "first_seen": 0,
    "last_seen": 8950
  },
  "person_1": {
    "appearances": 320,
    "frames": [100, 102, 104, ...],
    "first_seen": 100,
    "last_seen": 7200
  }
}
```

**활용 예:**
```python
import json

# 결과 로드
with open("tracking_results_20250112_143022.json", "r") as f:
    data = json.load(f)

# 가장 많이 출연한 인물 찾기
max_person = max(data.items(), key=lambda x: x[1]['appearances'])
print(f"주인공: {max_person[0]} ({max_person[1]['appearances']}회)")

# 출연 시간 계산 (30fps 가정)
for person_id, info in data.items():
    duration_seconds = (info['last_seen'] - info['first_seen']) / 30
    print(f"{person_id}: {duration_seconds:.1f}초 구간에 출연")
```

## 문제 해결

### 문제 1: GUI가 실행되지 않음

**증상:**
- 창이 나타나지 않음
- 오류 메시지 표시

**해결 방법:**
```bash
# 1. 의존성 확인
python test_installation.py

# 2. Tkinter 설치 확인 (Linux)
sudo apt-get install python3-tk

# 3. 직접 실행
python gui_app.py
```

### 문제 2: 웹캠이 작동하지 않음

**원인:**
- 다른 프로그램이 카메라 사용 중
- 카메라 권한 없음
- 카메라가 연결되지 않음

**해결 방법:**
1. 다른 프로그램(Zoom, Skype 등) 종료
2. 카메라 권한 확인
3. 카메라 연결 상태 확인
4. 다른 카메라 시도 (여러 개 있는 경우)

### 문제 3: 처리 속도가 너무 느림

**해결 방법:**
1. **프레임 스킵 증가**: 5-10으로 설정
2. **영상 해상도 낮추기**:
   ```bash
   ffmpeg -i input.mp4 -vf scale=640:360 input_small.mp4
   ```
3. **tolerance 조정**: 0.65-0.7로 설정

### 문제 4: 인식 정확도가 낮음

**증상:**
- 같은 사람이 여러 ID로 분할
- 다른 사람이 같은 ID로 인식

**해결 방법:**

**같은 사람 → 여러 ID:**
- tolerance를 **증가**: 0.65-0.7
- 조명이 일정한 환경 사용
- 정면 얼굴이 잘 보이도록

**다른 사람 → 같은 ID:**
- tolerance를 **감소**: 0.5-0.55
- 프레임 스킵 감소 (더 정확한 분석)

### 문제 5: 메모리 부족

**증상:**
- 프로그램이 느려지거나 멈춤
- 오류 메시지: MemoryError

**해결 방법:**
1. 프레임 스킵 증가
2. 영상을 여러 부분으로 나누어 처리
3. 프로그램 재시작 후 다시 시도

## 키보드 단축키

현재 버전에서는 단축키를 지원하지 않지만, 향후 버전에서 추가될 예정:
- `Space`: 일시정지/재개
- `S`: 결과 저장
- `R`: 리셋
- `Esc`: 종료

## 성능 벤치마크

### 일반적인 성능 (Intel i5, 8GB RAM 기준)

| 설정 | FPS | 정확도 | 권장 용도 |
|------|-----|--------|----------|
| frame_skip=1, tolerance=0.6 | 8-12 | 최고 | 짧은 영상 정밀 분석 |
| frame_skip=2, tolerance=0.6 | 15-20 | 높음 | 실시간 웹캠 |
| frame_skip=5, tolerance=0.6 | 25-30 | 중상 | 일반 영상 파일 |
| frame_skip=10, tolerance=0.65 | 30+ | 중간 | 긴 영상 빠른 처리 |

### 최적 설정 추천

**케이스 1: 실시간 회의 모니터링**
```
프레임 스킵: 2-3
정확도: 0.6
예상 FPS: 15-25
```

**케이스 2: 영화/드라마 분석**
```
프레임 스킵: 5-10
정확도: 0.6
예상 FPS: 25-30
```

**케이스 3: 보안 영상 분석**
```
프레임 스킵: 3-5
정확도: 0.55 (엄격)
예상 FPS: 15-20
```

## 팁과 요령

### 💡 Tip 1: 최적의 조명
- 밝고 균일한 조명 사용
- 역광 피하기
- 그림자 최소화

### 💡 Tip 2: 카메라 각도
- 정면 얼굴이 가장 잘 인식됨
- 45도 이상 각도는 인식률 저하
- 카메라를 눈높이에 배치

### 💡 Tip 3: 배치 처리
여러 영상을 순차적으로 처리:
```python
from realtime_tracker import RealtimePersonTracker

videos = ["video1.mp4", "video2.mp4", "video3.mp4"]

for i, video in enumerate(videos):
    tracker = RealtimePersonTracker(frame_skip=5)
    tracker.start(video)
    # 처리 완료까지 대기
    tracker.save_results(f"results_{i}.json")
```

### 💡 Tip 4: 결과 분석
결과를 엑셀이나 Python으로 추가 분석:
```python
import pandas as pd
import json

with open("results.json") as f:
    data = json.load(f)

# DataFrame으로 변환
df = pd.DataFrame([
    {"person": k, **v}
    for k, v in data.items()
])

# 분석
print(df.sort_values('appearances', ascending=False))
```

## 다음 단계

- CLI 버전도 사용해보기 (`cli.py`)
- Python API로 커스텀 분석 도구 만들기
- TUTORIAL.md에서 더 많은 예제 확인

질문이나 문제가 있으면 GitHub Issues에 남겨주세요!
